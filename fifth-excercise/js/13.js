// Method vs Function

const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // parseInt es una funcion porque si vemos la sintaxis tenemos el nombre de la funcion seguido de un parentesis y ademas recibe un valor

console.log(numero1.toString()); //toString es un metodo porque tiene un punto seguido del nombre de la funcion